export class ComposicionDetalladaClass {
  id: number;
  nombre: string;
  monto: number;
  constructor(id: number, nombre: string, monto: number) {
    this.id = id;
    this.nombre = nombre;
    this.monto = monto;
  }
}
